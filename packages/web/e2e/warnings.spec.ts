// Manual-mode warning suppression tests were removed along with full_manual mode.
// Warning infrastructure (WarningModal, suppressible codes) is tested indirectly
// through other e2e flows that trigger engine errors.
