name: CI

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: spell
          POSTGRES_PASSWORD: spell
          POSTGRES_DB: spell
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U spell -d spell"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://spell:spell@localhost:5432/spell

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release tag format
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid tag '$TAG'. Expected format: vMAJOR.MINOR.PATCH (example: v1.2.3)"
            exit 1
          fi

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run DB migrations
        run: bun run --cwd packages/db db:migrate

      - name: Lint
        run: bun run lint

      - name: Format check
        run: bun run format:check

      - name: Typecheck all packages
        run: bun run typecheck

      - name: Run tests with coverage
        run: |
          bun test --cwd packages/engine --coverage
          bun test --cwd packages/data --coverage
          bun --env-file=.env test --cwd packages/db --coverage
          bun --env-file=.env test --cwd packages/api --coverage

      - name: Install Playwright Chromium
        run: bunx playwright install --with-deps chromium

      - name: Run web E2E tests (auth bypass)
        run: bun run test:web

      - name: Run web E2E tests (auth mode)
        run: bun run test:web:auth-mock

      - name: Build web
        run: bun run build
